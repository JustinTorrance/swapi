import React from 'react'; 
import { shallow, mount } from 'enzyme';
import CardContainer from './CardContainer.js';

describe('CardContainer', () => {
  it('should do this', () => {
    // const wrapper = shallow(<CardContainer title="Bananas" />); 

  });
});